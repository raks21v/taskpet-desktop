{
  "name": "taskpets-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "TaskPets Windows desktop pet application",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.3"
  }
}
